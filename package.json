{
  "name": "spotify-profile-widget",
  "version": "1.0.0",
  "description": "Custom Spotify Now Playing widget for GitHub profile",
  "main": "api/now-playing.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "querystring": "^0.2.1"
  }
}
